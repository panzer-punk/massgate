services:
  # mysql:
  #   image: mysql:8.0
  #   ports:
  #     - 3306:3306
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
  #     MYSQL_DATABASE: ${DB_DATABASE}
  #     MYSQL_USER: ${DB_USER}
  #     MYSQL_PASSWORD: ${DB_PASSWORD}
  #   volumes:
  #     - ./docker/mysql/01_init_db.sh:/docker-entrypoint-initdb.d/01_init_db.sh
  #     - ./docker/mysql/02_databasestructure.sql:/docker-entrypoint-initdb.d/02_databasestructure.sql
  #     - mysql_data:/var/lib/mysql
  #   networks:
  #     massgate_network:
  #         aliases:
  #           - readdb.massgate.local
  #           - writedb.massgate.local

  web:
    build:
      context: .
      dockerfile: docker/web.Dockerfile
    ports:
      - 8099:80
    volumes:
      - ./docker/web/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/web/www-root:/usr/share/nginx/html:ro
    networks:
      - massgate_network

  # massgate:
  #   build:
  #     context: .
  #     dockerfile: docker/massgate.Dockerfile
  #   ports:
  #     - 3001:3001
  #   networks:
  #     - massgate_network
  #   depends_on:
  #     - mysql
  #     - web

networks:
  massgate_network:
    driver: bridge

volumes:
  mysql_data:
